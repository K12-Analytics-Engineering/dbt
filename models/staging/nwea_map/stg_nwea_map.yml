
version: 2


models:
  - name: nwea_map_edfi_assessments
    config:
      schema: staging
      materialized: table

  - name: nwea_map_edfi_objective_assessments
    config:
      schema: staging
      materialized: table

  - name: nwea_map_edfi_student_assessments
    config:
      schema: staging
      materialized: table

  - name: stg_nwea_map_assessment_results
    config:
      schema: staging
      materialized: table
